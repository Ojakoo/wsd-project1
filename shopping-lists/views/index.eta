<% layout("./layout.eta") %>

<h1>Shared shopping lists</h1>
<div>
  <% if (it.list_count === 0) { %>
    <h3>No shopping lists yet.</h3>
  <% } else { %>
    <h3>Shopping lists: <%= it.list_count %><br/>
    Shopping list items: <%= it.item_count %></h3>
  <% } %>
</div>
